# TODO

- [ ] Project save/load (`.json`) so drafts are portable across browsers/devices.
- [ ] Unsaved-change guard on refresh/tab close when there are pending edits.
- [ ] Cue import/export (`.json` and optional `.srt`/`.vtt`) for draft reuse across reels.
